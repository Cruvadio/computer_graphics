#include <stdio.h>
#include <stdlib.h>
#include <GL/glew.h>
#include <GLFW/glfw3.h>
#include <glm/glm.hpp>

using namespace glm;

int main (int argc, char** argv)
{
  if (!glfwInit())
  {
    fprintf(stderr, "Error while initializing glfw");
    return 1;
  }

  
}
